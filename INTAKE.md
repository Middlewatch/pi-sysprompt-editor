# Intake

One entry per real-use annoyance. Each entry records: the complaint in the
owner's terms, a Chesterton check (why the current behavior exists), the
spec implication if any, and the resolution.

Entries land newest first.
