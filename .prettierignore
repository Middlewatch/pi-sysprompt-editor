# Plans carry the packet field format the execute skill parses, and
# fixtures must stay byte-stable for golden and output-test comparability.
# Templates are owner prose that becomes the system prompt verbatim; the
# formatter never reflows them.
plans/
fixtures/
templates/
