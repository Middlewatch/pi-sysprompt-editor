# Design review, template manager basis (2026-08-17)

Independent fresh-context review of the draft `DESIGN.md` and its working
scope doc, structure-first brief per the design-basis skill.

## Findings

- **STR-01 (material, closed)**: the Structure section presented the `lib/`
  decomposition and `templates/` directory as an unqualified tree
  description while the repo today is a single `index.ts` with `tests/`.
  Fixed: the section now states the current layout and labels the
  decomposition as the build target.
- **PUB-01 (material, partially closed)**: ratification-session language in
  DESIGN.md. "The ratified build adds" in Expected behaviors and the
  session-narrative provenance bullet are rewritten in durable language.
  The status banner's pointer to open ratification items stays while the
  document is draft, because the design-basis convention requires it; it
  comes out at ratification.

## Passes

Section order, thesis falsifiability, doctrine dating, and internal
consistency between the scope doc's rulings and the basis all passed
cleanly. See the scope doc for the rulings themselves.
